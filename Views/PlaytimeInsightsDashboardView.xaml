<UserControl x:Class="PlaytimeInsights.Views.PlaytimeInsightsDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:PlaytimeInsights.Converters"
             xmlns:controls="clr-namespace:PlaytimeInsights.Controls"
             xmlns:services="clr-namespace:PlaytimeInsights.Services"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1200"
             KeyboardNavigation.TabNavigation="Continue"
             Foreground="{DynamicResource TextBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/PlaytimeInsightsVisualResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        <converters:CoverImageConverter x:Key="CoverImageConverter" />
        <sys:Double x:Key="TextOpacityPrimary">1</sys:Double>
        <sys:Double x:Key="TextOpacitySecondary">0.72</sys:Double>
        <sys:Double x:Key="TextOpacityTertiary">0.58</sys:Double>
        <sys:Double x:Key="TextOpacityDisabled">0.45</sys:Double>
        <LinearGradientBrush x:Key="WeekdaySelectedBackgroundBrush"
                             StartPoint="0.5,0"
                             EndPoint="0.5,1">
            <GradientStop Color="#334A90E2" Offset="0" />
            <GradientStop Color="#1A4A90E2" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="WeekdaySelectedBorderBrush"
                             StartPoint="0,0.5"
                             EndPoint="1,0.5">
            <GradientStop Color="#FF4A90E2" Offset="0" />
            <GradientStop Color="#FF8B5CF6" Offset="1" />
        </LinearGradientBrush>
        <SolidColorBrush x:Key="WeekdaySelectedIndicatorBrush" Color="#FF60A5FA" />
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelSeparatorBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>
        <Style x:Key="MetricCardStyle"
               TargetType="Border"
               BasedOn="{StaticResource PanelStyle}">
            <Setter Property="Background" Value="{StaticResource MetricCardBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MetricCardRimLightBorderBrush}" />
            <Setter Property="MinHeight" Value="154" />
            <Setter Property="Padding" Value="16" />
        </Style>
        <Style x:Key="HeroMetricCardStyle"
               TargetType="Border"
               BasedOn="{StaticResource PanelStyle}">
            <Setter Property="Background" Value="{StaticResource MetricCardBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MetricCardRimLightBorderBrush}" />
            <Setter Property="MinHeight" Value="176" />
            <Setter Property="Padding" Value="20" />
        </Style>
        <Style x:Key="ModulePanelStyle"
               TargetType="Border"
               BasedOn="{StaticResource PanelStyle}">
            <Setter Property="Background" Value="{StaticResource ModuleBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ModuleRimLightBorderBrush}" />
        </Style>
        <Style x:Key="DrilldownCardStyle"
               TargetType="Border"
               BasedOn="{StaticResource PanelStyle}">
            <Setter Property="Background" Value="{StaticResource DrilldownCardBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MetricCardRimLightBorderBrush}" />
            <Setter Property="Padding" Value="0" />
        </Style>
        <Style x:Key="RankingItemCardStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelSeparatorBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource RankingHoverBackgroundBrush}" />
                </Trigger>
                <DataTrigger Binding="{Binding Position}" Value="1">
                    <Setter Property="Background" Value="{StaticResource RankingGoldGlowBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Position}" Value="2">
                    <Setter Property="Background" Value="{StaticResource RankingSilverGlowBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Position}" Value="3">
                    <Setter Property="Background" Value="{StaticResource RankingBronzeGlowBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RankingTopHighlightStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="IsHitTestVisible" Value="False" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Position}" Value="1">
                    <Setter Property="Background" Value="{StaticResource RankingGoldTopHighlightBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DrilldownSessionItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource DrilldownSessionItemBackgroundBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="8,10" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DrilldownSessionHoverBackgroundBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="GhostIconButtonStyle"
               TargetType="{x:Type Button}">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#14FFFFFF" />
            <Setter Property="BorderBrush" Value="#1AFFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="GhostButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="GhostButtonBorder"
                                        Property="Background"
                                        Value="#264A90E2" />
                                <Setter TargetName="GhostButtonBorder"
                                        Property="BorderBrush"
                                        Value="#664A90E2" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="GhostButtonBorder"
                                        Property="Background"
                                        Value="#334A90E2" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="GhostButtonBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource TextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HeroMetricValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="34" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="HeroMetricMinorValueStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource HeroMetricValueStyle}">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="10,0,0,2" />
        </Style>
        <Style x:Key="HeroMetricUnitStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="4,0,0,5" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacitySecondary}" />
        </Style>
        <Style x:Key="MetricHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacitySecondary}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="MetricIconStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacityTertiary}" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="IsHitTestVisible" Value="False" />
        </Style>
        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="MetricHelperTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacityTertiary}" />
            <Setter Property="Margin" Value="0,4,0,0" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <SolidColorBrush x:Key="TrendIncreaseBrush" Color="#FF3FA46A" />
        <SolidColorBrush x:Key="TrendIncreaseFillBrush" Color="#243FA46A" />
        <SolidColorBrush x:Key="TrendDecreaseBrush" Color="#FF4E8FD1" />
        <SolidColorBrush x:Key="TrendDecreaseFillBrush" Color="#244E8FD1" />
        <LinearGradientBrush x:Key="ChartBarBrush"
                             StartPoint="0.5,1"
                             EndPoint="0.5,0">
            <GradientStop Color="#FF2457D6" Offset="0" />
            <GradientStop Color="#FF2F8CFF" Offset="0.62" />
            <GradientStop Color="#FFA45CFF" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="HeatmapActiveBrush"
                             StartPoint="0,1"
                             EndPoint="1,0">
            <GradientStop Color="#FF2457D6" Offset="0" />
            <GradientStop Color="#FFA45CFF" Offset="1" />
        </LinearGradientBrush>
        <Style x:Key="TrendTagStyle" TargetType="Border">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelSeparatorBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="9" />
            <Setter Property="Padding" Value="7,2" />
            <Setter Property="Margin" Value="0,3,0,0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrendKind}" Value="Increase">
                    <Setter Property="Background" Value="{StaticResource TrendIncreaseFillBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource TrendIncreaseBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding TrendKind}" Value="Decrease">
                    <Setter Property="Background" Value="{StaticResource TrendDecreaseFillBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource TrendDecreaseBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TrendTagTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrendKind}" Value="Increase">
                    <Setter Property="Foreground" Value="{StaticResource TrendIncreaseBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding TrendKind}" Value="Decrease">
                    <Setter Property="Foreground" Value="{StaticResource TrendDecreaseBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacitySecondary}" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style x:Key="HelpIconButtonStyle"
               TargetType="{x:Type Button}"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="19" />
            <Setter Property="Height" Value="19" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="7,0,0,0" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelSeparatorBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacitySecondary}" />
            <Setter Property="Cursor" Value="Help" />
            <Setter Property="ToolTipService.ShowDuration" Value="60000" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="AdvancedFilterExpanderStyle"
               TargetType="{x:Type Expander}">
            <Setter Property="Foreground" Value="{StaticResource RankingEnergyBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelSeparatorBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="0,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Expander}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <ToggleButton x:Name="HeaderSite"
                                              Background="Transparent"
                                              BorderBrush="Transparent"
                                              BorderThickness="0"
                                              Foreground="{TemplateBinding Foreground}"
                                              HorizontalContentAlignment="Left"
                                              Padding="{TemplateBinding Padding}"
                                              IsChecked="{Binding IsExpanded,
                                                  RelativeSource={RelativeSource TemplatedParent},
                                                  Mode=TwoWay}"
                                              Focusable="True">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter Content="{TemplateBinding Content}"
                                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                    <StackPanel Orientation="Horizontal">
                                        <ContentPresenter Content="{TemplateBinding Header}"
                                                          ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                          VerticalAlignment="Center" />
                                        <TextBlock Text="▾"
                                                   Margin="6,0,0,0"
                                                   VerticalAlignment="Center"
                                                   IsHitTestVisible="False" />
                                    </StackPanel>
                                </ToggleButton>
                                <ContentPresenter x:Name="ExpandSite"
                                                  Grid.Row="1"
                                                  ContentSource="Content"
                                                  Margin="{TemplateBinding Padding}"
                                                  Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite"
                                        Property="Visibility"
                                        Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RankingBadgeStyle" TargetType="Border">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="CornerRadius" Value="15" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelSeparatorBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Position}" Value="1">
                    <Setter Property="BorderBrush" Value="{StaticResource RankingGoldBrush}" />
                    <Setter Property="Background" Value="{StaticResource RankingGoldFillBrush}" />
                    <Setter Property="BorderThickness" Value="2" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Position}" Value="2">
                    <Setter Property="BorderBrush" Value="{StaticResource RankingSilverBrush}" />
                    <Setter Property="Background" Value="{StaticResource RankingSilverFillBrush}" />
                    <Setter Property="BorderThickness" Value="2" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Position}" Value="3">
                    <Setter Property="BorderBrush" Value="{StaticResource RankingBronzeBrush}" />
                    <Setter Property="Background" Value="{StaticResource RankingBronzeFillBrush}" />
                    <Setter Property="BorderThickness" Value="2" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RankingPositionTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Opacity" Value="{StaticResource TextOpacitySecondary}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Position}" Value="1">
                    <Setter Property="Foreground" Value="{StaticResource RankingGoldBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Position}" Value="2">
                    <Setter Property="Foreground" Value="{StaticResource RankingSilverBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Position}" Value="3">
                    <Setter Property="Foreground" Value="{StaticResource RankingBronzeBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <DataTemplate x:Key="GameRankingItemTemplate">
            <Border Height="64"
                    Margin="0,2"
                    Style="{StaticResource RankingItemCardStyle}"
                    ToolTip="{Binding ProgressTooltipText}">
                <Grid>
                <Grid Margin="8,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="38" />
                        <ColumnDefinition Width="42" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource RankingBadgeStyle}">
                        <TextBlock Text="{Binding Position}"
                                   Style="{StaticResource RankingPositionTextStyle}" />
                    </Border>
                    <Border Grid.Column="1"
                            Width="36"
                            Height="50"
                            HorizontalAlignment="Center"
                            Background="{DynamicResource PopupBackgroundBrush}"
                            BorderBrush="{DynamicResource PanelSeparatorBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            ClipToBounds="True">
                        <Image Source="{Binding CoverImagePath, Converter={StaticResource CoverImageConverter}}"
                               Stretch="UniformToFill"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               Focusable="False"
                               IsHitTestVisible="False" />
                    </Border>
                    <ProgressBar Grid.ColumnSpan="4"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding ProgressPercent}"
                                 VerticalAlignment="Stretch"
                                 HorizontalAlignment="Stretch"
                                 Style="{StaticResource RankingEnergyBackgroundBarStyle}" />
                    <StackPanel Grid.Column="2"
                                Margin="12,0"
                                VerticalAlignment="Center"
                                ClipToBounds="True">
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding DetailText}"
                                       FontSize="10"
                                       Opacity="{StaticResource TextOpacityTertiary}"
                                       TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <TextBlock Grid.Column="3"
                               Text="{Binding PrimaryValueText}"
                               VerticalAlignment="Center"
                               Margin="12,0,4,0"
                               FontWeight="SemiBold"
                               Opacity="{StaticResource TextOpacityPrimary}" />
                </Grid>
                    <Border Style="{StaticResource RankingTopHighlightStyle}"
                            Height="1"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Stretch"
                            SnapsToDevicePixels="True" />
                </Grid>
            </Border>
        </DataTemplate>
        <Style x:Key="WeekdayButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid x:Name="ButtonRoot" RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="SelectionTransform" Y="0" />
                            </Grid.RenderTransform>
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <Ellipse x:Name="SelectionIndicator"
                                     Width="5"
                                     Height="5"
                                     Margin="0,6,6,0"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Top"
                                     Fill="{StaticResource WeekdaySelectedIndicatorBrush}"
                                     Visibility="Collapsed"
                                     Focusable="False"
                                     IsHitTestVisible="False" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource ControlBackgroundBrush}" />
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource PanelSeparatorBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource TextBrush}" />
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderThickness"
                                        Value="1" />
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{StaticResource WeekdaySelectedBackgroundBrush}" />
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="{StaticResource WeekdaySelectedBorderBrush}" />
                                <Setter TargetName="ButtonBorder"
                                        Property="BorderThickness"
                                        Value="1" />
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF4A90E2"
                                                          BlurRadius="12"
                                                          ShadowDepth="2"
                                                          Direction="270"
                                                          Opacity="0.32" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="SelectionIndicator"
                                        Property="Visibility"
                                        Value="Visible" />
                                <Setter Property="Foreground" Value="#FFFFFFFF" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="Panel.ZIndex" Value="2" />
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionTransform"
                                                             Storyboard.TargetProperty="Y"
                                                             To="-2"
                                                             Duration="0:0:0.12"
                                                             DecelerationRatio="0.8" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionTransform"
                                                             Storyboard.TargetProperty="Y"
                                                             To="0"
                                                             Duration="0:0:0.12"
                                                             AccelerationRatio="0.35" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder"
                                        Property="Opacity"
                                        Value="{StaticResource TextOpacityDisabled}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer x:Name="DashboardScrollViewer"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24,22,24,24">
            <Grid Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Background="{DynamicResource GlyphBrush}"
                            CornerRadius="2"
                            Margin="0,2,0,2" />
                    <StackPanel Grid.Column="1" Margin="14,0,0,0">
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsName}"
                                   FontSize="28"
                                   FontWeight="SemiBold" />
                    </StackPanel>
                </Grid>
                <Button Grid.Column="1"
                        Content="{DynamicResource LOCPlaytimeInsightsRefreshButton}"
                        AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsRefreshButton}"
                        Padding="18,7"
                        VerticalAlignment="Center"
                        Command="{Binding RefreshCommand}" />
            </Grid>

            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,18">
                <StackPanel>
                    <DockPanel Margin="0,0,0,14">
                        <Border DockPanel.Dock="Right"
                                Background="{DynamicResource PopupBackgroundBrush}"
                                BorderBrush="{DynamicResource PanelSeparatorBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="10,5"
                                Margin="16,0,4,0">
                            <TextBlock Text="{Binding RangeText}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                        </Border>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource LOCPlaytimeInsightsAnalysisRange}"
                                       Style="{StaticResource SectionTitleStyle}"
                                       VerticalAlignment="Center" />
                            <Button Content="?"
                                    Style="{StaticResource HelpIconButtonStyle}"
                                    AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                                    ToolTip="{DynamicResource LOCPlaytimeInsightsCustomDateHint}" />
                        </StackPanel>
                    </DockPanel>
                    <WrapPanel>
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,0,16,8"
                                    VerticalAlignment="Bottom">
                            <Button Content="{DynamicResource LOCPlaytimeInsightsQuick7Days}"
                                    AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsQuick7Days}"
                                    Margin="0,0,6,0"
                                    Command="{Binding SelectRangeCommand}"
                                    CommandParameter="{x:Static services:DateRangePreset.Last7Days}" />
                            <Button Content="{DynamicResource LOCPlaytimeInsightsQuick30Days}"
                                    AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsQuick30Days}"
                                    Margin="0,0,6,0"
                                    Command="{Binding SelectRangeCommand}"
                                    CommandParameter="{x:Static services:DateRangePreset.Last30Days}" />
                            <Button Content="{DynamicResource LOCPlaytimeInsightsQuick1Year}"
                                    AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsQuick1Year}"
                                    Margin="0,0,6,0"
                                    Command="{Binding SelectRangeCommand}"
                                    CommandParameter="{x:Static services:DateRangePreset.ThisYear}" />
                            <Button Content="{DynamicResource LOCPlaytimeInsightsQuickAll}"
                                    AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsQuickAll}"
                                    Command="{Binding SelectRangeCommand}"
                                    CommandParameter="{x:Static services:DateRangePreset.AllSessions}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="{DynamicResource LOCPlaytimeInsightsDateRange}" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox ItemsSource="{Binding RangeOptions}"
                                      AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsDateRange}"
                                      SelectedItem="{Binding SelectedRangeOption, Mode=TwoWay}"
                                      DisplayMemberPath="Label"
                                      Width="132" />
                        </StackPanel>
                        <WrapPanel Visibility="{Binding CustomDateVisibility}">
                            <StackPanel Margin="0,0,16,8">
                                <TextBlock Text="{DynamicResource LOCPlaytimeInsightsStartDate}" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding CustomStartDate, Mode=TwoWay}"
                                            AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsStartDate}"
                                            Width="150" />
                            </StackPanel>
                            <StackPanel Margin="0,0,16,8">
                                <TextBlock Text="{DynamicResource LOCPlaytimeInsightsEndDate}" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding CustomEndDate, Mode=TwoWay}"
                                            AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsEndDate}"
                                            Width="150" />
                            </StackPanel>
                        </WrapPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="{DynamicResource LOCPlaytimeInsightsRankingMetric}" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox ItemsSource="{Binding RankingMetricOptions}"
                                      AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsRankingMetric}"
                                      SelectedItem="{Binding SelectedRankingMetricOption, Mode=TwoWay}"
                                      DisplayMemberPath="Label"
                                      Width="132" />
                        </StackPanel>
                    </WrapPanel>
                    <Expander x:Name="AdvancedFilterExpander"
                              Style="{StaticResource AdvancedFilterExpanderStyle}"
                              IsExpanded="True">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource LOCPlaytimeInsightsAdvancedFilters}" />
                                <Border Margin="8,0,0,0"
                                        Visibility="{Binding Filter.ActiveMetadataFilterVisibility}">
                                    <TextBlock Text="{Binding Filter.ActiveMetadataFilterSummary}" />
                                </Border>
                            </StackPanel>
                        </Expander.Header>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{DynamicResource LOCPlaytimeInsightsFilterDimension}"
                                           Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox ItemsSource="{Binding MetadataDimensionOptions}"
                                          AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsFilterDimension}"
                                          SelectedItem="{Binding SelectedMetadataDimensionOption, Mode=TwoWay}"
                                          DisplayMemberPath="Label" />
                            </StackPanel>
                            <StackPanel Grid.Column="2"
                                        Visibility="{Binding MetadataValueVisibility}">
                                <TextBlock Text="{DynamicResource LOCPlaytimeInsightsFilterValue}"
                                           Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox ItemsSource="{Binding MetadataValueOptions}"
                                          AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsFilterValue}"
                                          SelectedItem="{Binding SelectedMetadataValueOption, Mode=TwoWay}"
                                          DisplayMemberPath="Label"
                                          MinWidth="160"
                                          MaxWidth="260" />
                            </StackPanel>
                        </Grid>
                    </Expander>
                </StackPanel>
            </Border>

            <Grid Margin="0,0,0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border x:Name="RangeDurationHeroCard">
                    <Border.Style>
                        <Style TargetType="Border"
                               BasedOn="{StaticResource HeroMetricCardStyle}">
                            <Setter Property="Grid.Row" Value="0" />
                            <Setter Property="Grid.Column" Value="0" />
                            <Setter Property="Grid.ColumnSpan" Value="1" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCompactHeroLayout, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                             Value="True">
                                    <Setter Property="Grid.Row" Value="0" />
                                    <Setter Property="Grid.Column" Value="0" />
                                    <Setter Property="Grid.ColumnSpan" Value="3" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsRangeDuration}"
                                   Style="{StaticResource MetricHeaderStyle}" />
                        <Grid Margin="0,12,0,0"
                              ToolTip="{Binding RangeDurationDisplay.AutomationText}"
                              AutomationProperties.Name="{Binding RangeDurationDisplay.AutomationText}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       MinWidth="0"
                                       Text="{Binding RangeDurationDisplay.MajorValue}"
                                       Style="{StaticResource HeroMetricValueStyle}" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding RangeDurationDisplay.MajorUnit}"
                                       Style="{StaticResource HeroMetricUnitStyle}" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding RangeDurationDisplay.MinorValue}"
                                       Style="{StaticResource HeroMetricMinorValueStyle}" />
                            <TextBlock Grid.Column="3"
                                       Text="{Binding RangeDurationDisplay.MinorUnit}"
                                       Style="{StaticResource HeroMetricUnitStyle}" />
                        </Grid>
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsPreciseSessionBasis}"
                                   Style="{StaticResource MetricHelperTextStyle}" />
                        <StackPanel VerticalAlignment="Bottom"
                                    Margin="0,10,0,0"
                                    Visibility="{Binding ComparisonVisibility}">
                            <Border Style="{StaticResource TrendTagStyle}"
                                    DataContext="{Binding PreviousPeriodComparison}"
                                    ToolTip="{Binding TooltipText}">
                                <TextBlock Text="{Binding TagText}"
                                           Style="{StaticResource TrendTagTextStyle}" />
                            </Border>
                            <Border Style="{StaticResource TrendTagStyle}"
                                    DataContext="{Binding YearOverYearComparison}"
                                    ToolTip="{Binding TooltipText}">
                                <TextBlock Text="{Binding TagText}"
                                           Style="{StaticResource TrendTagTextStyle}" />
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border x:Name="SessionCountHeroCard">
                    <Border.Style>
                        <Style TargetType="Border"
                               BasedOn="{StaticResource HeroMetricCardStyle}">
                            <Setter Property="Grid.Row" Value="0" />
                            <Setter Property="Grid.Column" Value="2" />
                            <Setter Property="Grid.ColumnSpan" Value="1" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCompactHeroLayout, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                             Value="True">
                                    <Setter Property="Grid.Row" Value="2" />
                                    <Setter Property="Grid.Column" Value="0" />
                                    <Setter Property="Grid.ColumnSpan" Value="3" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsSessionCount}"
                                   Style="{StaticResource MetricHeaderStyle}" />
                        <TextBlock Text="{Binding SessionCountText}"
                                   Margin="0,12,0,0"
                                   Style="{StaticResource HeroMetricValueStyle}"
                                   AutomationProperties.Name="{Binding SessionCountText}" />
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsOverlappingSessions}"
                                   Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
            </Grid>

            <controls:ResponsiveUniformPanel
                                             Margin="0,0,0,18"
                                             MinItemWidth="204"
                                             PreferredItemWidth="232"
                                             MaxItemWidth="300"
                                             MinColumns="1"
                                             MaxColumns="4"
                                             HorizontalSpacing="12"
                                             VerticalSpacing="12"
                                             CenterIncompleteRow="True">
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricActivityBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE787;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricActivityForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsActiveDays}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <TextBlock Text="{Binding ActiveDaysText}" Style="{StaticResource MetricValueStyle}" />
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsActiveNaturalDays}" Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricSessionBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE8EF;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricSessionForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsAverageSession}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       MinWidth="0"
                                       Text="{Binding AverageSessionDisplay.MajorValue}"
                                       Style="{StaticResource MetricValueStyle}" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding AverageSessionDisplay.MajorUnit}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Bottom"
                                       Margin="4,0,0,5"
                                       Opacity="{StaticResource TextOpacitySecondary}" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding AverageSessionDisplay.MinorValue}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       VerticalAlignment="Bottom"
                                       Margin="10,0,0,2" />
                            <TextBlock Grid.Column="3"
                                       Text="{Binding AverageSessionDisplay.MinorUnit}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Bottom"
                                       Margin="4,0,0,5"
                                       Opacity="{StaticResource TextOpacitySecondary}" />
                        </Grid>
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsAverageFormula}" Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricDurationBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE740;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricDurationForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsLongestSession}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       MinWidth="0"
                                       Text="{Binding LongestSessionDisplay.MajorValue}"
                                       Style="{StaticResource MetricValueStyle}" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding LongestSessionDisplay.MajorUnit}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Bottom"
                                       Margin="4,0,0,5"
                                       Opacity="{StaticResource TextOpacitySecondary}" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding LongestSessionDisplay.MinorValue}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       VerticalAlignment="Bottom"
                                       Margin="10,0,0,2" />
                            <TextBlock Grid.Column="3"
                                       Text="{Binding LongestSessionDisplay.MinorUnit}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Bottom"
                                       Margin="4,0,0,5"
                                       Opacity="{StaticResource TextOpacitySecondary}" />
                        </Grid>
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsClippedToRange}" Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricDurationBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE81C;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricDurationForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsLifetimeDuration}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       MinWidth="0"
                                       Text="{Binding LifetimeDurationDisplay.MajorValue}"
                                       Style="{StaticResource MetricValueStyle}" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding LifetimeDurationDisplay.MajorUnit}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Bottom"
                                       Margin="4,0,0,5"
                                       Opacity="{StaticResource TextOpacitySecondary}" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding LifetimeDurationDisplay.MinorValue}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       VerticalAlignment="Bottom"
                                       Margin="10,0,0,2" />
                            <TextBlock Grid.Column="3"
                                       Text="{Binding LifetimeDurationDisplay.MinorUnit}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Bottom"
                                       Margin="4,0,0,5"
                                       Opacity="{StaticResource TextOpacitySecondary}" />
                        </Grid>
                        <TextBlock Text="{Binding TrackedDurationSummaryText}" Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricActivityBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE8EE;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricActivityForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsLongestStreak}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <TextBlock Text="{Binding LongestStreakText}" Style="{StaticResource MetricValueStyle}" />
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsLongestStreakHint}" Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricActivityBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE72C;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricActivityForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsCurrentStreak}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <TextBlock Text="{Binding CurrentStreakText}" Style="{StaticResource MetricValueStyle}" />
                        <TextBlock Text="{Binding CurrentStreakDateText}"
                                   ToolTip="{DynamicResource LOCPlaytimeInsightsCurrentStreakHint}"
                                   Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MetricCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    Background="{StaticResource MetricAnomalyBackgroundBrush}"
                                    VerticalAlignment="Center">
                                <TextBlock Text="&#xE7BA;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{StaticResource MetricAnomalyForegroundBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{DynamicResource LOCPlaytimeInsightsAnomalyHints}"
                                       Style="{StaticResource MetricHeaderStyle}" />
                        </Grid>
                        <TextBlock Text="{Binding AnomalyCountText}" Style="{StaticResource MetricValueStyle}" />
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsAnomalyReadOnly}" Style="{StaticResource MetricHelperTextStyle}" />
                    </StackPanel>
                </Border>
            </controls:ResponsiveUniformPanel>
            <controls:AdaptiveDashboardPanel
                EnterWideWidth="1200"
                ExitWideWidth="1160"
                SecondaryColumnRatio="0.38"
                ColumnSpacing="18"
                VerticalSpacing="18">

            <Border x:Name="TrendModule"
                    Style="{StaticResource ModulePanelStyle}"
                    controls:AdaptiveDashboardPanel.Zone="Primary">
                <StackPanel>
                    <DockPanel Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal"
                                    DockPanel.Dock="Left">
                            <TextBlock Text="{Binding PeriodTitleText}"
                                       FontSize="18"
                                       FontWeight="SemiBold" />
                            <Button Content="?"
                                    Style="{StaticResource HelpIconButtonStyle}"
                                    AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                                    ToolTip="{DynamicResource LOCPlaytimeInsightsPeriodChartHint}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource LOCPlaytimeInsightsChartGranularity}"
                                       Style="{StaticResource FieldLabelStyle}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0" />
                            <ComboBox ItemsSource="{Binding AggregationOptions}"
                                      AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsChartGranularity}"
                                      SelectedItem="{Binding SelectedAggregationOption, Mode=TwoWay}"
                                      DisplayMemberPath="Label"
                                      Width="110"
                                      VerticalAlignment="Center" />
                        </StackPanel>
                    </DockPanel>
                    <TextBlock Text="{DynamicResource LOCPlaytimeInsightsTrendLine}"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <controls:AdaptiveTrendChart ItemsSource="{Binding PeriodActivities}"
                                                 Height="230"
                                                 MinWidth="320"
                                                 Cursor="Cross"
                                                 PeriodSelected="AdaptiveTrendChart_PeriodSelected" />
                </StackPanel>
            </Border>

            <Border x:Name="RankingModule"
                    Style="{StaticResource ModulePanelStyle}"
                    controls:AdaptiveDashboardPanel.Zone="Secondary">
                <TabControl>
                    <TabItem Header="{DynamicResource LOCPlaytimeInsightsRangeRankingTab}"
                             AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsRangeRankingTab}">
                        <ItemsControl ItemsSource="{Binding RangeGameRankings}"
                                      ItemTemplate="{StaticResource GameRankingItemTemplate}" />
                    </TabItem>
                    <TabItem Header="{DynamicResource LOCPlaytimeInsightsLifetimeRankingTab}"
                             AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsLifetimeRankingTab}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LOCPlaytimeInsightsLifetimeRankingScope}"
                                       Margin="0,0,0,12"
                                       TextWrapping="Wrap"
                                       Opacity="{StaticResource TextOpacityTertiary}" />
                            <ItemsControl ItemsSource="{Binding LifetimeGameRankings}"
                                          ItemTemplate="{StaticResource GameRankingItemTemplate}" />
                        </StackPanel>
                    </TabItem>
                </TabControl>
            </Border>

            <Border x:Name="DistributionModule"
                    Style="{StaticResource ModulePanelStyle}"
                    controls:AdaptiveDashboardPanel.Zone="Primary">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsTimeDistribution}"
                                   FontSize="18"
                                   FontWeight="SemiBold" />
                        <Button Content="?"
                                Style="{StaticResource HelpIconButtonStyle}"
                                AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                                ToolTip="{DynamicResource LOCPlaytimeInsightsDistributionDescription}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsWeekdayDistribution}"
                                   FontWeight="SemiBold" />
                        <Button Content="?"
                                Style="{StaticResource HelpIconButtonStyle}"
                                AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                                ToolTip="{DynamicResource LOCPlaytimeInsightsWeekdayFilterHint}" />
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding WeekdayDistribution}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="7" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Tag="{Binding}"
                                        Command="{Binding DataContext.SelectWeekdayCommand,
                                            RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="{Binding AutomationName}"
                                        ToolTip="{Binding TooltipText}"
                                        Cursor="Hand"
                                        Style="{StaticResource WeekdayButtonStyle}"
                                        Margin="4,0"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="110" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border Width="42"
                                                Height="{Binding BarHeight}"
                                                VerticalAlignment="Bottom"
                                                HorizontalAlignment="Center"
                                                Background="{StaticResource ChartBarBrush}"
                                                CornerRadius="7,7,0,0"
                                                ToolTip="{Binding TooltipText}" />
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding Label}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,5,0,0" />
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{Binding HourDistributionTitle}" FontWeight="SemiBold" Margin="0,20,0,8" />
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="NestedScrollViewer_PreviewMouseWheel">
                        <ItemsControl ItemsSource="{Binding HourDistribution}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Width="42" Margin="1,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="108" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border Width="24"
                                                Height="{Binding BarHeight}"
                                                VerticalAlignment="Bottom"
                                                HorizontalAlignment="Center"
                                                Background="{StaticResource ChartBarBrush}"
                                                CornerRadius="6,6,0,0"
                                                ToolTip="{Binding TooltipText}" />
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding Label}"
                                                   HorizontalAlignment="Center"
                                                   FontSize="9"
                                                   Opacity="{StaticResource TextOpacityTertiary}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <TextBlock Text="{DynamicResource LOCPlaytimeInsightsWeekHourHeatmap}" FontWeight="SemiBold" Margin="0,20,0,8" />
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="NestedScrollViewer_PreviewMouseWheel"
                                  HorizontalContentAlignment="Center">
                        <Grid HorizontalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="20" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ItemsControl Grid.Column="1"
                                          ItemsSource="{Binding AdvancedHourLabels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Width="32"
                                                   FontSize="9"
                                                   HorizontalAlignment="Center"
                                                   Opacity="{StaticResource TextOpacityTertiary}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl Grid.Row="1"
                                          ItemsSource="{Binding AdvancedWeekdayLabels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Height="32"
                                                   VerticalAlignment="Center"
                                                   FontSize="10"
                                                   Opacity="{StaticResource TextOpacityTertiary}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl Grid.Row="1"
                                          Grid.Column="1"
                                          ItemsSource="{Binding WeekHourCells}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="24" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="30"
                                                Height="30"
                                                Margin="1"
                                                CornerRadius="3"
                                                Background="Transparent"
                                                BorderBrush="{DynamicResource PanelSeparatorBrush}"
                                                BorderThickness="1"
                                                ToolTip="{Binding TooltipText}">
                                            <Grid>
                                                <Border Background="{DynamicResource TextBrush}"
                                                        Opacity="0.06"
                                                        IsHitTestVisible="False" />
                                                <Border CornerRadius="2"
                                                        Background="{StaticResource HeatmapActiveBrush}"
                                                        Opacity="{Binding HeatOpacity}"
                                                        IsHitTestVisible="False" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsCalendarHeatmap}"
                                   FontSize="18"
                                   FontWeight="SemiBold" />
                        <Button Content="?"
                                Style="{StaticResource HelpIconButtonStyle}"
                                AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                                ToolTip="{DynamicResource LOCPlaytimeInsightsCalendarHint}" />
                    </StackPanel>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="NestedScrollViewer_PreviewMouseWheel"
                                  HorizontalContentAlignment="Center">
                        <Grid HorizontalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="46" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ItemsControl ItemsSource="{Binding HeatmapWeekdayLabels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Height="26"
                                                   VerticalAlignment="Center"
                                                   FontSize="10"
                                                   Opacity="{StaticResource TextOpacityTertiary}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl Grid.Column="1"
                                          ItemsSource="{Binding HeatmapCells}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="{Binding HeatmapColumnCount}" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="24"
                                                Height="24"
                                                Margin="1"
                                                CornerRadius="3"
                                                Visibility="{Binding CellVisibility}"
                                                Background="Transparent"
                                                BorderBrush="{DynamicResource PanelSeparatorBrush}"
                                                BorderThickness="1"
                                                ToolTip="{Binding TooltipText}"
                                                Tag="{Binding}"
                                                Cursor="Hand"
                                                MouseLeftButtonUp="HeatmapCell_MouseLeftButtonUp">
                                            <Grid>
                                                <Border Background="{DynamicResource TextBrush}"
                                                        Opacity="0.06"
                                                        IsHitTestVisible="False" />
                                                <Border CornerRadius="2"
                                                        Background="{StaticResource HeatmapActiveBrush}"
                                                        Opacity="{Binding HeatOpacity}"
                                                        IsHitTestVisible="False" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <Border x:Name="AnomalyModule"
                    Style="{StaticResource ModulePanelStyle}"
                    controls:AdaptiveDashboardPanel.Zone="Secondary"
                    Visibility="{Binding AnomalyVisibility}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="{DynamicResource LOCPlaytimeInsightsAnomalyReviewTitle}"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                        <Button Content="?"
                                Style="{StaticResource HelpIconButtonStyle}"
                                AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                                ToolTip="{DynamicResource LOCPlaytimeInsightsAnomalyDescription}" />
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Anomalies}"
                             MaxHeight="300"
                             HorizontalContentAlignment="Stretch"
                             PreviewMouseWheel="NestedScrollViewer_PreviewMouseWheel"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="42" MinWidth="760" Margin="0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="260" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding GameName}"
                                               VerticalAlignment="Center"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding StartedText}"
                                               VerticalAlignment="Center"
                                               Opacity="{StaticResource TextOpacitySecondary}" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding DurationText}"
                                               VerticalAlignment="Center"
                                               Opacity="{StaticResource TextOpacitySecondary}" />
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding Reason}"
                                               VerticalAlignment="Center"
                                               Opacity="{StaticResource TextOpacityPrimary}"
                                               TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border x:Name="DrilldownModule"
                    Style="{StaticResource DrilldownCardStyle}"
                    controls:AdaptiveDashboardPanel.Zone="Primary"
                    IsVisibleChanged="DrilldownModule_IsVisibleChanged"
                    Visibility="{Binding SessionDetailVisibility}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0"
                            Background="{StaticResource DrilldownAccentBrush}"
                            CornerRadius="4,0,0,4"
                            SnapsToDevicePixels="True"
                            IsHitTestVisible="False" />
                    <StackPanel Grid.Column="1" Margin="15,18,18,18">
                        <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedDetailTitle}"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,4"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding SessionDetailCountText}"
                                       FontSize="11"
                                       Opacity="{StaticResource TextOpacityTertiary}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                Style="{StaticResource GhostIconButtonStyle}"
                                Margin="12,0,0,0"
                                VerticalAlignment="Top"
                                Command="{Binding ClearDrilldownSelectionCommand}"
                                ToolTip="{DynamicResource LOCPlaytimeInsightsClearSelection}"
                                AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsClearSelection}">
                            <TextBlock Text="&#xE711;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="11"
                                       Opacity="{StaticResource TextOpacitySecondary}"
                                       IsHitTestVisible="False" />
                        </Button>
                    </Grid>
                    <Border Height="1"
                            Margin="0,0,0,6"
                            Background="{StaticResource DrilldownDividerBrush}"
                            SnapsToDevicePixels="True" />
                    <ListView ItemsSource="{Binding SessionDetails}"
                               Style="{StaticResource PlaytimeInsightsDetailListStyle}"
                               ItemContainerStyle="{StaticResource PlaytimeInsightsDetailItemStyle}"
                               MinHeight="160"
                               MaxHeight="380"
                               HorizontalContentAlignment="Stretch"
                               VerticalContentAlignment="Top"
                               Background="Transparent"
                               BorderThickness="0"
                               PreviewMouseWheel="NestedScrollViewer_PreviewMouseWheel"
                               ScrollViewer.VerticalScrollBarVisibility="Auto"
                               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                               ScrollViewer.CanContentScroll="True"
                               VirtualizingPanel.IsVirtualizing="True"
                               VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Height="66"
                                        Margin="0,0,0,6"
                                        Style="{StaticResource DrilldownSessionItemStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Width="36"
                                                Height="50"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Background="Transparent"
                                                CornerRadius="4"
                                                ClipToBounds="True">
                                            <Image Source="{Binding CoverImagePath,
                                                Converter={StaticResource CoverImageConverter}}"
                                                   Stretch="UniformToFill"
                                                   RenderOptions.BitmapScalingMode="HighQuality"
                                                   Focusable="False"
                                                   IsHitTestVisible="False" />
                                        </Border>
                                        <Grid Grid.Column="2">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="{Binding GameName}"
                                                       FontSize="13"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource DrilldownGameNameBrush}"
                                                       TextTrimming="CharacterEllipsis" />
                                            <Grid Grid.Row="1" Margin="0,6,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding StartedText}"
                                                           FontSize="11"
                                                           VerticalAlignment="Center"
                                                           Foreground="{StaticResource DrilldownTimestampBrush}"
                                                           TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding DurationText}"
                                                           FontSize="12"
                                                           FontWeight="SemiBold"
                                                           Margin="12,0,0,0"
                                                           VerticalAlignment="Center"
                                                           Foreground="{StaticResource DrilldownDurationBrush}" />
                                                <Border Grid.Column="2"
                                                        Margin="12,0,0,0"
                                                        VerticalAlignment="Center"
                                                        Style="{StaticResource SessionSourceTagStyle}"
                                                        AutomationProperties.Name="{Binding SourceText}">
                                                    <TextBlock Text="{Binding SourceText}"
                                                               FontSize="10"
                                                               TextTrimming="CharacterEllipsis" />
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Button Content="{DynamicResource LOCPlaytimeInsightsLoadMore100}"
                            AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsLoadMore100}"
                            Visibility="{Binding LoadMoreVisibility}"
                            HorizontalAlignment="Center"
                            Padding="20,7"
                            Margin="0,10,0,0"
                            Command="{Binding LoadMoreSessionDetailsCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            </controls:AdaptiveDashboardPanel>

            <DockPanel Margin="0,18,0,0">
                <Button DockPanel.Dock="Right"
                        Content="?"
                        Style="{StaticResource HelpIconButtonStyle}"
                        AutomationProperties.Name="{DynamicResource LOCPlaytimeInsightsHelp}"
                        ToolTip="{DynamicResource LOCPlaytimeInsightsDataBasisNote}" />
                <TextBlock Text="{Binding StatusText}"
                           AutomationProperties.Name="{Binding StatusText}"
                           Opacity="{StaticResource TextOpacityPrimary}"
                           VerticalAlignment="Center" />
            </DockPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
