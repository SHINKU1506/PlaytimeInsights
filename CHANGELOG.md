# Changelog

## 0.9.3 — 2026-07-28

- 新增项目根目录 `.gitignore` 和发布检查清单，隔离构建、暂存、安装包、IDE 与用户运行数据；
- README 中的本机源码位置改用 `%AppData%` 表示，避免公开文档携带具体用户名；
- 删除聚合柱形图，只保留单一自适应折线/面积趋势图；
- 新增原生 `AdaptiveTrendChart`，曲线、面积、节点和 X 轴标签按控件实际宽度实时计算，不再横向滚动；
- X 轴标签按可用宽度自动稀疏化，并始终保留最后一个周期标签；
- 鼠标悬停显示贯穿绘图区的垂直 Crosshair、锚点和半透明数据卡，展示周期、最多三个游戏名及总时长；
- 点击当前 Crosshair 周期继续使用原会话下钻；
- 日粒度达到 90 个周期后隐藏常驻节点，达到 90/180 个周期后线宽分别降为 1.5/1 像素；
- 周期游戏摘要复用当前库名称，游戏删除后回退会话快照；超过三个游戏使用本地化“等/etc.”后缀；
- 修正自绘控件曾把趋势线误改为单一 `#4A90E2`：恢复 `#2F8CFF → #A45CFF` 蓝紫渐变线，
  面积层恢复蓝紫半透明到透明的垂直渐隐；
- X 轴标签改为按 `FormattedText.Width` 实际测量并进行碰撞检测；为最后一个日期提前预留宽度，
  相邻标签不足 8 像素时跳过候选，修复日期聚合偶发重叠；
- 新增统一 19 × 19 原生 `?` 帮助按钮，将分析范围、异常、分布口径、星期筛选、日历、趋势交互、
  数据口径和会话导入安全说明折叠为长时 Tooltip；
- 未选择趋势/热力格时不再显示空下钻面板及“未显示会话”；选择后才显示真实标题、数量和列表；
- Tooltip 按钮提供本地化无障碍名称，关键状态、错误、当前范围和真实计数继续常驻；
- 版本、程序集和界面副标题升级为 0.9.3；中英资源各 272 个键。

## 0.9.2 — 2026-07-28

- 修复日历热力图和“星期 × 小时”热力图在 Playnite English 下仍显示中文星期的问题；
- 新增统一 `WeekdayLabelService`，星期名称完全由插件语言资源提供，不再读取 Windows `CurrentCulture`；
- 新增 Windows 地区与 Playnite 语言交叉回归；
- 英文月份标题改用插件资源内的数字格式，避免中文 Windows 地区在英文界面显示中文月份；
- 新增工作区尺寸约束，编辑与导入窗口在 100%、125%、150%、200% DPI 下保持可见；
- 编辑窗口改为可缩放并支持垂直滚动；
- 仪表盘、会话管理、会话明细、异常列表和导入预览增加窄窗口横向溢出与长文本换行策略；
- 修复弹窗使用不存在的 `WindowBackgroundBrush`，改用默认主题和 Seaside 均支持的 `PopupBackgroundBrush`；
- 设置校验、导入预览、CSV/JSON 解析和备份恢复的主要错误路径完成中英本地化；
- 本地化回归新增源码键覆盖和格式化占位符一致性检查；
- 增加 0.1–0.9 旧设置默认值矩阵，并继续覆盖 schema 1–4 无损数据升级；
- 新增独立侧边栏图标：分析入口使用“时钟 + 柱形图”，会话入口使用“列表 + 时钟”；
- 图标以 64 × 64 RGBA PNG 发布，并完成 Seaside 主题下的实际侧栏缩放与入口对应检查；
- 星期分布改为可点击/键盘选择，联动筛选下方 24 小时分布；再次选择同一天恢复全部星期；
- 新增动态小时分布标题、星期选中态、中英操作提示和屏幕阅读器名称；
- 重新整理两个原生 WPF 主页面的标题、面板、字段标签和间距，保持 Playnite 动态主题且不引入 WebView；
- 分析范围摘要改为右上角主题信息块，星期选择态改用主题描边，避免浅色高亮覆盖整块柱形图；
- 会话页重排常用操作与数据工具层级，并为列表新增固定列头、行分隔、悬停和左侧主题色选中反馈；
- 修正自定义会话行容器的前景色继承，深色主题显式使用 `TextBrush`；
- 中英资源扩充到各 267 个键，并保存当前客户端的审查前后截图及同尺寸对照；
- 修复调整 Playnite 窗口大小时“Playnite 累计总时长”指标卡失去右边距、与下一张卡片黏连的问题；
- 区分星期按钮的选中态与键盘焦点态，再次点击恢复总体小时分布后不再残留蓝色选中条；
- 为指标卡统一间距以及星期焦点/选中边框厚度增加静态回归；
- 区间与累计排名新增 Playnite 本地库封面缩略图，固定为 36 × 50，并以 96 像素 OnLoad 解码避免锁定媒体文件；
- 排名前三名新增金、银、铜圆形数字勋章；
- 两套排名新增半透明原生进度条，表达游戏时长占当前统计口径全部游戏总时长的比例；
- 进度条分母包含 Top N 之外的游戏；切换为会话数等排名指标时，进度仍保持时长占比语义；
- 新增中英文占比 tooltip 以及封面路径、转换器、奖牌资源和进度绑定静态回归；
- 重构顶部 9 张指标卡：主数字统一提升为 26pt 粗体，辅助说明统一使用 `#8A8A8A`，
  每张卡片右上角增加 `Segoe MDL2 Assets` 单色语义图标；
- 将独立环比/同比面板合并到“区间游玩时长”卡片底部；趋势 Tag 显示方向、绝对时长差和
  环比/同比，增长使用绿色、下降使用蓝色，原百分比和对比区间保留在 tooltip；
- 卡片统一为 218 × 154，继续使用 WrapPanel 和 12 像素间距，窗口缩放时保持稳定换行；
- 修复“当前连续游玩”合并显示截止日期后主数字被截断：主值只保留连续天数，截止日期移入
  11pt 辅助说明，完整口径提示保留在 tooltip；
- 星期分布移除每根柱子下方的常驻时长文字；星期与 24 小时柱体统一使用顶部圆角蓝紫渐变，
  具体时长只在悬浮时显示；
- 星期 × 小时热力格从 24 放大到 30 像素，日历热力格从 18 放大到 24 像素；两者在可用空间内
  居中，溢出时继续使用原生水平滚动；
- 两张热力图的零时长格使用 `#2A2A2E` 底色和细描边，活跃强度叠加蓝紫渐变，形成类似
  GitHub 贡献图的连续网格；
- 趋势折线改为单调三次贝塞尔曲线，并在曲线下方增加由约 40% 到 0% 的垂直渐变面积；
  圆点 Tooltip、键盘/鼠标点击和周期会话钻取保持不变；
- 按日聚合柱新增独立视觉方案：`Rectangle RadiusX/RadiusY=2`，填充从 `#4A90E2`
  渐变到透明；周/月/年聚合继续使用 Playnite 主题色，便于实机对比；
- 会话钻取从无列头的自由 Grid 行改为原生 `ListView + GridView`，固定为游戏 260、开始时间 170、
  游玩时长 120、会话来源 110 像素，并提供对应本地化列头；
- 排名进度从名称下方 5 像素细条改为整条列表项的底层背景，使用 `#4A90E2` 和 0.12 透明度；
  `PART_Track/PART_Indicator` 保证填充宽度严格对应 `ProgressPercent`，前景内容不受透明度影响；
- 自动化回归扩充到 58 项，Release 构建保持 0 警告、0 错误。

## 0.9.1 — 2026-07-28

- 新增完整 `en_US.xaml` 与 `zh_CN.xaml`，英文作为内置安全回退，Playnite 当前地区资源覆盖回退；
- 仪表盘、会话管理、会话编辑、导入预览、设置页、侧边栏及原生文件/确认对话框迁入本地化资源；
- 日期范围、聚合标题、时长、排名、对比、连续天数、异常原因、筛选标签和操作状态支持动态本地化；
- 新增容错 `LocalizationService`，在 Playnite 资源未初始化或键缺失时回退到中文基线；
- 按钮增加访问键，并消除会话管理页内访问键冲突；
- 五个原生视图增加显式 Tab 顺序、初始焦点、循环键盘导航和屏幕阅读器自动化名称；
- 图表、热力图和状态区保留文字冗余，不依赖颜色单独传达时长或状态；
- 新增中英资源键集合/非空校验及原生视图本地化/无障碍静态回归；
- 自动化回归扩充到 51 项，Release 构建保持 0 警告、0 错误。

## 0.9.0 — 2026-07-27

- 进入发布候选阶段，新增隐私、诊断、大型数据和升级验证基础；
- 会话管理页新增“保存诊断报告”，由用户主动选择本地文本文件；
- 诊断报告包含插件/schema 版本、会话分类数量、文件大小、备份数量和存储状态；
- 诊断报告不包含游戏名称、会话时间、用户路径、游戏 ID 或会话 ID；
- 设置页新增原生隐私与诊断说明；
- 新增随插件发布的 `PRIVACY.md`；
- `SessionRepository.GetStorageDiagnostics` 提供线程安全、只读的存储摘要；
- 小时分配服务缓存已解析时区，并为无夏令时时区使用直接整点换算；
- 5,000 个游戏、10 万条会话、十年范围完整分析约 300 毫秒；
- schema 4 JSON 的 10 万条会话加载约 1,136 毫秒；
- 两项压力测试均设置 30 秒发布预算；
- 新增 schema 1、2、3、4 到当前 schema 4 的会话身份、归属、名称和秒数无损升级回归；
- 自动化回归扩充到 49 项，Release 构建保持 0 警告、0 错误。

## 0.8.4 — 2026-07-27

- 分析仪表盘新增库来源、开发者、类型、标签和安装状态筛选；
- 库来源复用会话管理页的 `LibraryPlugin` 名称映射，并区分手动添加和未知/未加载库；
- 筛选统一作用于区间指标、所有图表、会话钻取、区间排名、累计总时长和累计排名；
- 新增原生星期分布柱形图；
- 新增原生 24 小时时段分布柱形图；
- 新增原生星期 × 小时二维热力图；
- 跨小时会话按本地时区的实际墙钟区段分片，包含夏令时重复/跳过小时处理，并保持总秒数守恒；
- 新增所选范围内最长连续游玩天数及截至范围结束日/今天的当前连续天数；
- 新增上一等长区间环比和去年同期同比，支持零基数的“新增/持平”展示；
- 新增异常会话只读提示，检测零秒、结束早于开始、未来开始时间、至少 18 小时及记录秒数显著大于墙钟时长；
- 异常提示最多显示最近 50 条，不自动修改或删除任何会话；
- 全部新增图表继续使用原生 WPF，不引入 WebView、第三方图表库或远程资源；
- 对比卡片只使用已验证的 Playnite 通用动态画刷，避免第三方主题缺少资源；
- 自动化回归扩充到 45 项。

## 0.8.3 — 2026-07-27

- 新增原生导入预览窗口，预览阶段不写入会话存储；
- 支持 Playtime Insights JSON、CSV 和完整备份 JSON 导入；
- 支持 GameActivity 单游戏 JSON 及会话明细 CSV 导入；
- GameActivity JSON `DateSession` 按 UTC 解释；CSV 本地时间按当前 Windows 时区回转 UTC；
- GameActivity CSV 自动识别逗号/分号/制表符以及中英文表头，并兼容旧式点分时间；
- CSV 使用支持双引号、转义引号和跨行字段的 RFC 4180 风格解析器；
- 导入前校验游戏名、时间范围和 365 天时长安全上限；
- 按稳定 ID 或“游戏 + 开始时间 ±2 秒 + 时长”去重；
- 同名游戏唯一时允许名称关联；同名冲突时报错，未匹配名称使用稳定外部 ID；
- 会话存储升级为 schema 4，新增 `ImportSource` 和 `ImportConfidence`；
- 导入提交前自动生成 `Backups/sessions.*.pre-import.json` 回滚副本；
- 新增完整手动备份与备份恢复；
- 恢复仅接受含 `ActiveSessions` 的完整备份，筛选导出 JSON 必须走安全导入流程；
- 恢复前强制生成 `pre-restore` 回滚副本，且不会复活备份中的旧运行检查点；
- 新增重建存储索引：规范化 schema、排序、修复 ID 并移除重复指纹；
- 重建前强制生成 `pre-reindex` 回滚副本；
- CSV 导出补齐软删除、修改审计和导入审计字段；
- 自动化回归扩充到 38 项。

## 0.8.2 — 2026-07-27

- 元数据筛选新增开发者维度；
- 会话存储升级为 schema 3，旧 schema 1/2 在内存中无损规范化；
- 新增原生会话补录窗口；
- 新增原生会话编辑，可修改游戏、当地开始日期/时间和精确持续秒数；
- 编辑窗口验证时间、持续秒数和夏令时无效本地时间；
- 新增软删除，分析与默认查询自动排除已删除会话；
- 新增“包含已删除”筛选、删除状态列和恢复操作；
- 删除、恢复和编辑记录修改时间与原因；
- Repository 查询返回会话副本，避免界面直接修改存储对象；
- 编辑保存前阻止与其他会话产生重复指纹；
- 首次写操作继续通过临时文件、原子替换和备份保存整个 schema 3 文档；
- 自动化测试扩展至 30 项。

## 0.8.1 — 2026-07-27

- 移除容易被误解为会话运行环境的“平台”筛选；
- 默认改为库来源筛选，通过 `Game.PluginId` 映射加载中的 `LibraryPlugin.Name`；
- 手动添加游戏和未加载库使用独立标签；
- 新增可切换元数据维度：Playnite 来源、发行商、标签、类型、分类和安装状态；
- 关键字搜索扩展到所有当前 Playnite 元数据；
- 会话列表最后一列改为库来源；
- 明确元数据筛选使用当前 Playnite 游戏记录，不是历史会话快照；
- 修复清空筛选值集合时 `SelectedItem=null` 触发递归刷新并重复追加选项；
- 增加筛选更新抑制和通用刷新重入保护；
- 元数据访问边界可注入，独立测试无需 Playnite 游戏数据库；
- 自动化测试扩展至 26 项。

## 0.8.0 — 2026-07-27

- 新增独立“Playtime Insights · 会话”原生侧边栏；
- 新增游戏名称、来源名称和平台关键字搜索；
- 新增会话来源筛选；
- 新增平台筛选，支持逗号分隔平台快照的拆分、去重和排序；
- 会话按开始时间最新优先显示；
- 会话管理列表每批 200 条并启用 Recycling 虚拟化；
- 新增当前筛选结果 CSV 导出，使用 UTF-8 BOM 和完整双引号转义；
- 新增当前筛选结果版本化 JSON 导出；
- 导出操作只读，不修改 schema 2 会话存储或备份；
- JSON 序列化边界可注入，独立测试无需 Playnite 运行时上下文；
- 自动化测试扩展至 24 项。

## 0.5.2 — 2026-07-27

- 新增默认选中的“自动（推荐）”聚合粒度；
- 今天、本周和本月自动按日聚合；
- 本年自动按月聚合，避免生成约 365 个柱形和折线点；
- 自定义范围 62 天以内按日、63–730 天按周、731–3650 天按月、更长范围按年；
- 用户手动选择日、周、月或年时始终优先，不受自动规则覆盖；
- 聚合标题标注“自动”，同时显示实际采用的粒度；
- 新增自动跨度边界和手动覆盖测试，测试总数增至 19 项。

## 0.5.1 — 2026-07-27

- 会话明细从普通 `ItemsControl` 改为限高 `ListBox`；
- 明细区域最大高度 380，条目过多时使用独立垂直滚动；
- 启用 `VirtualizingStackPanel` 和 Recycling 容器复用；
- 首批仅显示 100 条，每次“加载更多”追加最多 100 条；
- 显示“已显示数量 / 总数量”；
- 新增 250 条数据的分页边界自动化测试，测试总数增至 17 项。

## 0.5.0 — 2026-07-27

- 新增原生日历热力图，并按当前周起点规则排列星期；
- 热力颜色按所选范围内的每日最大时长线性缩放；
- 新增与当前日/周/月/年粒度共享数据的原生折线趋势；
- 柱形图、折线点和热力格新增精确时长 tooltip；
- 柱形图、折线点和热力格均可点击钻取；
- 新增会话明细区域，展示游戏、当地开始时间、区间内时长和记录来源；
- 跨日期范围钻取仅展示会话落入所选范围的秒数；
- 整套图表继续使用 WPF 原生控件，不加载 WebView 或远程资源；
- 自动化测试扩展至 16 项。

## 0.3.1 — 2026-07-27

- 开始日期和结束日期选择器仅在时间范围设为“自定义”时显示；
- 今天、本周、本月和本年范围下日期选择器折叠且不占布局空间。

## 0.3.0 — 2026-07-27

- 新增今天、本周、本月、本年和自定义日期范围；
- 新增日、周、月、年四种精确会话聚合粒度；
- 新增区间时长、会话数、活跃天数、平均会话和最长会话指标；
- 新增区间游戏排名，并支持按时长、会话次数、活跃天数、平均会话和最长会话切换；
- 保留独立的 Playnite 累计总时长与所有游戏累计排名；
- 默认采用 ISO 8601 周一作为周起点，可切换为系统地区规则；
- 自定义日期倒序时自动纠正；
- 跨午夜会话只将落入所选日期范围的秒数计入统计；
- 自动化测试扩展至 12 项。

## 0.2.0 — 2026-07-27

- 短时长使用秒级精确显示，累计分钟采用四舍五入，消除一分钟显示歧义；
- active session 写入 `sessions.json`；
- 游戏运行期间每分钟刷新恢复检查点；
- Playnite 异常退出后自动恢复到最后检查点；
- 正常关闭时补全尚未收到停止事件的 active session；
- 跨午夜会话按本地时区拆分到多个自然日；
- 保存时记录时区、游戏来源和平台快照；
- 防止重复停止事件生成重复会话；
- 主 JSON 损坏时尝试从备份加载；
- 主文件和备份均不可读时进入保护状态，不覆盖损坏数据；
- 新增 7 个独立自动化测试。

## 0.1.0 — 2026-07-27

- 初始 GenericPlugin；
- 原生 WPF 侧边栏仪表盘；
- `OnGameStarted` / `OnGameStopped` 会话记录；
- Playnite 累计时长指标和排名；
- 最近 N 天原生柱状图；
- JSON 原子保存和设置页。
