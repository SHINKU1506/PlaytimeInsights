# Playtime Insights 隐私说明

更新日期：2026-07-27

Playtime Insights 是离线优先的 Playnite 插件。

## 本地处理

- 游戏会话、统计、筛选、导入、导出和异常检测均在本机完成；
- 插件不包含遥测、分析 SDK、广告、网络上传或远程网页；
- 插件不请求账号密码，也不读取 Playnite 之外的浏览历史或在线账户数据。

## 本地存储

精确会话保存在 Playnite 为插件分配的专属 `ExtensionsData` 目录。内容包括游戏 ID、记录时的
游戏名称快照、UTC 开始/结束时间、持续秒数、来源、时区偏移及编辑/导入审计字段。

插件使用临时文件、原子替换和本地备份降低数据损坏风险。导入、完整恢复和重建前会创建本地回滚备份。

## 用户主动创建的文件

只有用户主动选择以下操作时，插件才会在所选路径创建文件：

- CSV/JSON 会话导出；
- 完整备份；
- 导入错误报告；
- 诊断报告。

诊断报告不包含游戏名称、会话时间、用户目录路径、游戏 ID 或会话 ID，仅包含：

- 插件和会话 schema 版本；
- 有效、运行中、已删除及各来源会话数量；
- 主数据与最近备份的存在状态和字节数；
- 回滚备份数量；
- 存储是否可写、是否从备份恢复。

插件不会自动上传诊断报告。用户在分享报告前仍应自行检查文件内容。

## 删除与迁移

卸载插件不会由插件主动删除会话数据。用户可以在确认不再需要备份后，手动删除对应
`ExtensionsData/7094cd6b-d3a4-41d0-b7c3-f0cc535a9efd` 目录。
